<Window x:Class="XeryonEtherCAT.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:XeryonEtherCAT.App.ViewModels"
        mc:Ignorable="d"
        Title="Xeryon EtherCAT Dashboard"
        Width="960"
        Height="640"
        d:DataContext="{d:DesignInstance Type=vm:MainWindowViewModel, IsDesignTimeCreatable=True}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Xeryon EtherCAT Dashboard" FontWeight="Bold" FontSize="22" />
            <Border Background="#0078D7" CornerRadius="4" Padding="12,4" Margin="12,0,0,0">
                <TextBlock Text="{Binding ConnectionStatus}" Foreground="White" />
            </Border>
            <TextBlock Text="{Binding CycleMetrics}" Margin="16,0,0,0" VerticalAlignment="Center" />
        </StackPanel>

        <Border Grid.Row="1" Padding="12" Background="#10161A" CornerRadius="6" Margin="0,12,0,0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Slave" VerticalAlignment="Center" Foreground="White" />
                <TextBox Text="{Binding SelectedSlave, Mode=TwoWay}" Width="60" Margin="8,0,0,0" />
                <TextBlock Text="Target" VerticalAlignment="Center" Foreground="White" Margin="12,0,0,0" />
                <TextBox Text="{Binding TargetPosition, Mode=TwoWay}" Width="100" Margin="8,0,0,0" />
                <TextBlock Text="Jog vel" VerticalAlignment="Center" Foreground="White" Margin="12,0,0,0" />
                <TextBox Text="{Binding JogVelocity, Mode=TwoWay}" Width="100" Margin="8,0,0,0" />
                <StackPanel Orientation="Horizontal" Margin="20,0,0,0">
                    <Button Content="Enable" Command="{Binding EnableCommand}" Margin="0,0,6,0" />
                    <Button Content="Disable" Command="{Binding DisableCommand}" Margin="0,0,6,0" />
                    <Button Content="Move" Command="{Binding MoveCommand}" Margin="0,0,6,0" />
                    <Button Content="Index" Command="{Binding IndexCommand}" Margin="0,0,6,0" />
                    <Button Content="Jog +" Command="{Binding JogPositiveCommand}" Margin="0,0,6,0" />
                    <Button Content="Jog -" Command="{Binding JogNegativeCommand}" Margin="0,0,6,0" />
                    <Button Content="Jog Stop" Command="{Binding JogStopCommand}" Margin="0,0,6,0" />
                    <Button Content="Halt" Command="{Binding HaltCommand}" Margin="0,0,6,0" />
                    <Button Content="Stop" Command="{Binding StopCommand}" Margin="0,0,6,0" />
                    <Button Content="Reset" Command="{Binding ResetCommand}" />
                </StackPanel>
            </StackPanel>
        </Border>

        <Grid Grid.Row="2" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Background="#1B2226" CornerRadius="6" Padding="12" Margin="0,0,12,0">
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Drives}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type vm:DriveStatusViewModel}">
                                <Border BorderBrush="#2C3E50" BorderThickness="0,0,0,1" Padding="8">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="60" />
                                            <ColumnDefinition Width="120" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Slave}" FontWeight="Bold" />
                                        <TextBlock Grid.Column="1" Text="{Binding Position}" />
                                        <TextBlock Grid.Column="2" Text="{Binding StatusText}" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Status events" FontWeight="Bold" VerticalAlignment="Center" />
                </StackPanel>

                <Border Grid.Row="1" Background="#10161A" CornerRadius="6" Padding="8" Margin="0,8,0,0">
                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding StatusEvents}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type vm:DriveEventViewModel}">
                                    <TextBlock Text="{Binding}" TextWrapping="Wrap" Margin="0,0,0,6" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>

                <Border Grid.Row="2" Background="#10161A" CornerRadius="6" Padding="8" Margin="0,8,0,0">
                    <StackPanel>
                        <TextBlock Text="MQTT bridge" FontWeight="Bold" />
                        <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                            <TextBlock Text="Host" VerticalAlignment="Center" />
                            <TextBox Text="{Binding MqttHost, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="140" Margin="8,0,0,0" />
                            <TextBlock Text="Port" VerticalAlignment="Center" Margin="12,0,0,0" />
                            <TextBox Text="{Binding MqttPort, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="80" Margin="8,0,0,0" />
                            <Button Content="{Binding MqttButtonText}" Command="{Binding ToggleMqttCommand}" Margin="12,0,0,0" />
                        </StackPanel>
                        <TextBlock Text="{Binding MqttStatus}" FontStyle="Italic" Margin="0,6,0,0" />
                    </StackPanel>
                </Border>

                <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,8,0,0">
                    <TextBlock Text="Logs" FontWeight="Bold" VerticalAlignment="Center" />
                    <CheckBox Content="Mirror to console" IsChecked="{Binding ShowConsoleLogs}" VerticalAlignment="Center" Margin="12,0,0,0" />
                    <Button Content="Clear" Command="{Binding ClearLogCommand}" Margin="12,0,0,0" />
                </StackPanel>

                <Border Grid.Row="4" Background="#10161A" CornerRadius="6" Padding="8" Margin="0,8,0,0">
                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding Logs}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type vm:LogEntryViewModel}">
                                    <StackPanel>
                                        <TextBlock Text="{Binding}" TextWrapping="Wrap" FontFamily="Consolas" />
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Window>
